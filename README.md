# SkyTark

A new workspace for the SkyTark project.

## Getting started

- Open `skytark.code-workspace` in VS Code
- Add your project files under `src/`

## Structure

- `src/` — source files
- `.gitignore` — ignored files
- `skytark.code-workspace` — VS Code workspace file
